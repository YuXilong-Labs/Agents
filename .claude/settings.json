{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'f=$(jq -r \".tool_input.file_path // empty\"); if echo \"$f\" | grep -q \"wk-im-developer/\"; then echo \"{\\\"systemMessage\\\": \\\"⚠️  wk-im-developer/ 是已弃用的 v2 旧版，计划近期删除。请改到 wk-im-dev/ 中进行修改。\\\"}\"; fi' 2>/dev/null || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
