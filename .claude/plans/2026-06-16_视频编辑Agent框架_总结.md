# 2026-06-16 视频编辑 Agent 框架 总结

分支：`wk-video-dev/feature-scaffold`（3 commits，未 push，未发 tag）。

## 完成内容

1. **生成器升级**（`tools/create-wk-agent.sh`）
   - `--out` 在仓库根下时，自动 upsert `.claude-plugin/marketplace.json`（复用首条记录的 repo URL，兼容内网 mirror）；`--no-register` 关闭；生成到仓库外自动跳过。
   - 残留扫描新增「domain prose to rewrite」提示，精确列出仍含 IM 名词的文件。
2. **模板隐私散文泛化（Tier B）**：wk-im-dev 模板里 8 处硬编码 `messageBody/msgContent/attachmentURL` 改为「以 `components.conf` 的 privacy 项为准」。IM 隐私名词现仅存于 `components.conf`（数据源）。
3. **新增 `wk-video-dev`**（VideoEditCore + VideoEditUI，v1.0.0）：manifest 驱动生成 + 自动注册 marketplace + 领域散文全面重写为视频编辑域。
4. **文档**：根 README 补「新增 Agent」端到端指南 + 纠正版本 + 登记新模块；tools/README 补注册说明；项目 CLAUDE.md 模块表更新。
5. **清理**：删除 stale 根 `AGENTS.md`，`.gitignore` 忽略根 `/AGENTS.md` 与 `/CLAUDE-FABLE-5.md`。

## 关键改动清单

| 类别 | 文件 |
|---|---|
| 生成器 | `tools/create-wk-agent.sh`（+84 行：marketplace upsert / domain hints / 入参） |
| 模板泛化 | `wk-im-dev/` 的 agents(dev/verifier)、codex/AGENTS.md、commands、skills(im-knowledge/im-review)、hooks/session-init、docs(architecture/feishu)、README |
| 新 agent | `manifests/video-edit.json` + `wk-video-dev/`（整树，54 文件） |
| 分发 | `.claude-plugin/marketplace.json`（+wk-video-dev entry） |
| 文档 | 根 `README.md`、`tools/README.md`、`CLAUDE.md` |
| 清理 | `.gitignore`、删 `AGENTS.md` |

## 验证结果

- dogfood `im.json` 重生成 vs `wk-im-dev/`：仅 4 文件差异（plugin.json×2 / CHANGELOG / components.conf 注释头）——Tier B 与生成器改动零漂移。
- `wk-im-dev` / `wk-video-dev` 各自 `verify.sh` 通过。
- 全部 `*.sh` `bash -n` 通过；plugin/marketplace JSON 合法。
- `install.sh --runtime codex` 安装后：`wk-video-dev --version` = 1.0.0、`doctor` 全 ok（effective runtime=codex、launcher/agent spec 就位）。
- 无 standalone `IM` 残留、无 `message-flow` 悬挂引用、无 CJK 空格残渣；`消息/会话` 仅余框架义（session / assistant-message / 飞书 API）。

## 风险与遗留

- **pod 名是占位**：`VideoEditCore/VideoEditUI/VideoEngineSDK` 与隐私词为合理默认。若真实工程不同 → 改 `manifests/video-edit.json` 重跑生成器，再按 diff 局部补领域散文。
- **未发 tag / 未 push**：Claude plugin 真实安装路径未验证（需先 push + 打 tag，跑 wk-im-dev CLAUDE.md 的步骤 3 端到端）。
- **wk-video-dev/docs/architecture.md** 的部分 mermaid 仍是旧安装流程图（其顶部 banner 已声明「待重绘」，与模板一致，非本次回归）。
- 本机已 `install` 了 wk-video-dev 到 `~/.wk-video-dev`（codex 离线可用）；不需要可 `bash wk-video-dev/scripts/uninstall.sh`。
- Tier C（生成器内 templatize skill 目录改名）未做——本次视频 agent 的目录改名是手工完成。

## 后续建议

1. 确认/修正真实 pod 名后（如需）重跑生成器。
2. push 分支 → 开 PR；合并后为 wk-video-dev 打 `v1.0.0` tag，跑端到端（Codex bootstrap + Claude plugin 双路径）。
3. 可选 Tier C：把 `skills/im-*` 目录改名 + `is_im_repo`→`is_component_repo` 收敛进生成器/模板，让未来新 agent 零手工。
4. 可选：把 `wk-code-refactor/` 也对齐新范式（删 `core/`、plugin-native）。

---

## 更新（2026-06-16 v1.0.0 发布）

- **真实 pod 名**：用户提供 `BTVideoRecorderKit`(core) + `BTVideoRecorderUIKit`(UI)，且为录制器域。已就地改名(不重跑生成器)，领域散文编辑→录制(采集/预览/编码/导出)。manifest 改名 `video-edit.json`→`video-recorder.json`，隐私词 `exportPath`→`outputURL`。
- **多 agent tag 约定**：plain `vX.Y.Z` 属 wk-im-dev(已占用 v1.0.0~v1.1.1)；wk-video-dev 用 **scoped 无斜杠 tag** `wk-video-dev-v1.0.0`(保证 bootstrap raw URL 不歧义)。约定写入 CLAUDE.md + CHANGELOG。
- **已发布**：main 推送至 `4404424`；tag `wk-video-dev-v1.0.0` 已 push。
- **端到端通过**（CLAUDE.md 步骤 3）：
  - 3.1 Codex：从 tag `curl bootstrap | bash --runtime codex` → launcher `1.0.0` ✓
  - 3.2 Claude plugin：`marketplace update` + `install wk-video-dev@yuxilong-agents` → `Version 1.0.0 enabled`；`doctor` 报 `[ok] Claude plugin wk-video-dev enabled` + `effective runtime: claude` ✓
- **本机现状**：已安装 wk-video-dev v1.0.0(Codex launcher `~/.wk-video-dev/` + Claude plugin enabled)，`wk-video-dev` 命令全局可用。

## 遗留（更新）

- SDK 字面量仍为占位 `VideoEngineSDK`——接入真实录制 SDK 后改 `manifests/video-recorder.json` 重跑生成器。
- 真机 / 真实 `BTVideoRecorderKit`·`BTVideoRecorderUIKit` 仓库联调待业务侧验证。
- `wk-video-dev/docs/architecture.md` 部分 mermaid 仍为旧安装流程图(banner 已声明待重绘，与模板一致)。

---

## 更新（2026-06-16 v1.0.1 — 据真实仓库校正）

实地探查 `/Users/yuxilong/Desktop/code/BaiTuPods/BTVideoRecorderKit` 与 `BTVideoRecorderUIKit`：

- **真实定位**：短视频**拍摄 + 编辑**组件（非纯录制/纯编辑）。UIKit = 拍摄/编辑 UI（Camera/Edit/Filter/Sticker/MusicPicker），Kit = 引擎核心 + Contract 抽象。
- **真实底层引擎（取代占位 VideoEngineSDK）**：默认美摄 `NvStreamingSdkCore`（`Classes/NvsEditor/NvsEngine`，`BTVideoEditorEngineNvs`，`NvsStreamingContext.verifySdkLicenseFile` 授权）；可选阿里 `AliVCSDK_UGC`（`Services/AliEditor`，`Ali` 子 spec）；字节 `BTBytedEffect` 美颜 + 阿里 `MNN` 推理。
- **公开入口**：BTRouter Target-Action（`Target_BTVideoRecorderUIKit`：`Action_openCameraController`/`startVideoCapture`/`editorViewController`/`triggerProEditExport`/`openMusicPicker` 等）。
- **依赖约束（核实 UIKit 0 处直连厂商 SDK）**：`components.conf` forbid_import → `NvStreamingSdkCore`/`NvsStreamingContext`/`AliVCSDK_UGC`。
- **隐私词（据真实代码：filePath/videoPath/outputPath 高频）**：`videoPath`/`outputPath`/`outputURL`/`deviceId`/`userId`/`token`，取代占位 `sourceURL`/`licenseKey`。
- **改动**：manifest + 两份 plugin.json + marketplace + 根 README/CLAUDE + 知识库(architecture/contracts/pipeline) + identity/约束散文，全部校正；版本 1.0.0→1.0.1。
- **发布**：main `be0a61f`；tag `wk-video-dev-v1.0.1`（v1.0.0 保留为历史）。
- **端到端（v1.0.1）通过**：Codex bootstrap → `1.0.1` + 真实 components.conf；Claude plugin `update 1.0.0→1.0.1` + doctor `enabled` / `runtime claude`。

## 遗留（更新）

- 真机/真实仓库编译联调待业务侧；阿里 `Ali` 子 spec 路径未实跑。
- 若后续 pod 改名或引擎调整，改 `manifests/video-recorder.json` 重跑生成器再补领域散文。
